{
  "name": "physim",
  "displayName": "Stormworks Physics Sensor Sim",
  "description": "Simulate Stormworks physics sensor outputs from a 3D gizmo, alongside LifeBoatAPI's microcontroller debugger.",
  "version": "1.1.1",
  "publisher": "shannon-toppo",
  "icon": "images/icon.png",
  "preview": true,
  "license": "MIT",
  "keywords": [
    "stormworks",
    "lifeboatapi",
    "lua",
    "microcontroller",
    "physics sensor"
  ],
  "bugs": {
    "url": "https://github.com/Shannon-toppo/PhySim/issues"
  },
  "homepage": "https://github.com/Shannon-toppo/PhySim#readme",
  "engines": {
    "vscode": "^1.62.0"
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false
    },
    "virtualWorkspaces": false
  },
  "categories": [
    "Debuggers",
    "Other"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Shannon-toppo/PhySim.git"
  },
  "main": "./out/extension.js",
  "extensionDependencies": [
    "NameousChangey.lifeboatapi"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "physim.open",
        "title": "PhySim: Open Physics Sensor Panel"
      },
      {
        "command": "physim.reset",
        "title": "PhySim: Reset Gizmo"
      },
      {
        "command": "physim.showLog",
        "title": "PhySim: Show Log"
      }
    ],
    "configuration": {
      "title": "Stormworks Physics Sensor Sim",
      "properties": {
        "physim.port": {
          "type": "number",
          "default": 14239,
          "description": "TCP port the extension listens on for the PhySim Lua client."
        },
        "physim.autoOpenOnSimulate": {
          "type": "boolean",
          "default": true,
          "description": "Auto-open the panel when LifeBoatAPI Simulate (F6) starts."
        },
        "physim.panel.openLocation": {
          "type": "string",
          "enum": [
            "beside",
            "newWindow"
          ],
          "enumDescriptions": [
            "Open the panel beside the active editor in the current window (default).",
            "Open the panel in a new floating window (requires VSCode 1.85+)."
          ],
          "default": "beside",
          "description": "Where to place the Physics Sensor panel when it opens."
        },
        "physim.autoInjectLibraryPath": {
          "type": "boolean",
          "default": true,
          "description": "Automatically add the bundled lua/ folder to lifeboatapi.stormworks.libs.libraryPaths for LifeBoatAPI workspaces."
        },
        "physim.monitors.useBuiltInOnWindows": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "**Experimental (Windows only).** Render the microcontroller's monitors inside the PhySim panel instead of launching LifeBoatAPI's `STORMWORKS_Simulator.exe`.\n\nPhySim's monitor view is its own implementation, with drawing rules fitted to in-game screenshots. It does not reproduce `screen.drawMap` terrain (filled with sea colour instead), alt touch (primary touch only), or the exe's input/output panels — drive the channels from the PhySim panel instead. Takes effect on the next **F6**.\n\nOn macOS the built-in monitors are always used and this setting is ignored."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile && npm run copy-three",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint .",
    "check:media": "tsc -p tsconfig.media.json",
    "test": "npm run compile && node --test \"test/**/*.test.mjs\"",
    "copy-three": "node ./scripts/copy-three.js",
    "postinstall": "npm run copy-three"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^18.0.0",
    "@types/three": "^0.160.0",
    "@types/vscode": "^1.62.0",
    "eslint": "^10.6.0",
    "fengari": "^0.1.5",
    "globals": "^17.7.0",
    "three": "^0.160.0",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.62.1"
  }
}
