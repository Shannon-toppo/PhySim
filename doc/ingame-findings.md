# 実機検証の結果（Stormworks v1.15.23 / Apple M5・RTX 4070Ti）

`tools/ingame/verifyA_shapes.lua` / `verifyB_color.lua` / `verifyC_circle.lua` を
ゲーム内で実行し、`tools/ingame/image/{appleM5,RTX4070Ti}/` のスクリーンショットを
解析した結果。2026-09-21 時点。

**結論：モニターの描画は GPU のラスタライズ規則そのもので、図形は Mac と Windows で
1画素も違わない。** A・B・C の全ページで両者の点灯画素集合が完全一致した。
`media/raster.js` はこの規則で書き直してあり、撮影した 15 ページ全部を画素単位で
再現する（`test/raster.test.mjs` がスクリーンショット由来の fixture と突き合わせる）。

Storm Code の `screen_raster.rs`（「正典」）は16角形の円と premultiply だけが部分的に
正しく、線・円の分割数・塗りの境界は実機と違う。

## 解析の方法と精度

スクリーンショットを緑の目盛で 96x96 の論理格子へ射影変換で戻す
（`tools/ingame/analysis/rectify.mjs`）。今回、較正を書き直した。

- 旧版は上端＋左端の目盛が前提で、**左目盛の無い B カードは較正できなかった**。
  Windows (8.6 screen px / 論理px) では 1x1 の目盛が 55 画素しかなく、`n >= 60` の
  足切りにも落ちていた。
- 新版は目盛の列を直線として探し（RANSAC）、上・下・左のどれがあっても使う。
- 隅で2本の目盛が融合した塊（左 y=95 と下 x=0 など）は重心がどちらにも属さない。
  C カードで worst 0.5px の外れ値になっていたのがこれで、隅の目盛は除外する。

| カード | 目盛 | residual mean | worst |
|---|---|---|---|
| A1–A6 | 上＋左 | 0.011–0.026px | 0.066px |
| B1–B3, B5–B7 | 上＋下 | 0.012–0.028px | 0.091px |
| C1–C9 | 上＋下＋左 | 0.012–0.035px | 0.103px |

全ページが 0.05px 以下。B4 は全面が単色で目盛が無いので較正しない（不要）。
点灯判定は輝度 150。点灯 236 / 消灯 65 以下にきれいに分かれる。

## 1. drawLine — diamond-exit 規則

画素 (px, py) は、スクリプト座標で (px, py) を中心とする開いたひし形
`|x−px| + |y−py| < 1/2` を持つ（ゲームの半画素オフセットで画素中心が整数に来る）。
**線分がそのひし形から出ていくとき、その画素が点灯する。** D3D / GL / Metal の
非アンチエイリアス線の規則そのもの。

ここから、前回の観察が全部出てくる。

- 終点の画素は点灯しない（線はそこで終わり、ひし形から出ない）。区間は `[始点, 終点)`。
  向きを逆にすると別の画素になる：`(2,5)→(6,5)` は 2..5、`(6,5)→(2,5)` は 3..6。
- 長さ 0.5 の線は1画素点灯する（ひし形の角にちょうど届いて出る）。0.3 は出ないので
  何も描かない。「1px 未満は描かない」という閾値は存在しない。
- ひし形の縁ちょうどを通る場合は、線全体を**副軸方向へ無限小だけずらして**決める。
  x 主軸の線は下へ、y 主軸の線は左へ。だから y の .5 は下の行、x の .5 は左の列に
  なる（A4 の「x は切り下げ、y は切り上げ」）。単一のずらし方向では A4 と A5 が
  両立せず、主軸で方向を分けて初めて全部一致した。

## 2. drawCircle — 半径で辺数が変わる多角形

**N = clamp(floor(r/2), 8, 16) 角形**。頂点は角度0（真右）から。

| r | 1–17 | 18–19 | 20–21 | 22 | 32 | 44 |
|---|---|---|---|---|---|---|
| N | 8 | 9 | 10 | 11 | 16 | 16 |

C1–C4（r=8..22 を1刻み）、C7（r=32）、C8（r=44）で、各円ごとに N と開始角を
総当たりし、全て誤差0で決まった。

- **未解決だった A1（r=22）は 11角形で誤差0。** 左右非対称なのは 11 が奇数だから。
- r=1..7 は八角形。r=3 が中点円と一致したのは偶然。
- r=2.4〜3.0 が全部描かれるのも、辺が1px未満でも線は描かれるから（1節）。
- 上限 16 は r=32 で floor(r/2)=16 に達し、r=44 でも 16。上限値が 16..21 のどこかは
  データでは決まらないが、16 が自然。
- 頂点の回り順（時計/反時計）は結果に効かず、データでは決まらない。

## 3. 塗り — 画素の角で標本化

塗りは点標本で、**画素 (px, py) の標本点がスクリプト座標のどこにあるか**で決まる。

| 命令 | 標本点 | 縁ちょうどの扱い |
|---|---|---|
| drawCircleF, drawRectF | (px, py) — 左上角 | x を右へ、y を下へ無限小ずらす |
| drawTriangleF | (px, py+1) — **左下角** | x を右へ、y を上へ無限小ずらす |

drawTriangleF だけ標本点が違う。A6 の3つの三角形（整数・+0.5・小数頂点）はこの規則で
全て誤差0、円塗りの規則だと 138 画素ずれる。逆に C9・A3 の塗り円は三角形の規則だと
328 画素ずれる。オフセットを 1/16 刻みで総当たりしても、両立する値は無かった。

- A6 の直角三角形 `(50,68),(70,68),(50,88)` が 19行（68..86）になるのはこのため。
  上端（y=68）はどちらの規則でも同じ行に来る。違いが出るのは下側の縁だけ。
- 面積0の三角形は何も描かない。

## 4. drawRectF

上の表どおり、列は `ceil(x) .. ceil(x+w)−1`（B6）。

| x, w | 20, 3 | 20.25, 3 | 20.5, 3 | 20.5, 3.5 | 20.75, 3 | 19.5, 3 | −0.5, 4 | 70, 0.4 | 60, 0 |
|---|---|---|---|---|---|---|---|---|---|
| 列 | 20–22 | 21–23 | 21–23 | 21–23 | 21–23 | 20–22 | 0–3 | 70 | なし |

幅 0.4 でも1列描かれる。行は B6 に小数の y が無いので、x と同じ規則を当てている
（**未検証**）。

## 5. drawRect — 4本の線のループ

`(x,y)→(x+w,y)→(x+w,y+h)→(x,y+h)→(x,y)` を線で引いたもの。B5 の8個全部が
画素単位で一致した。

- **大きさは (w+1)×(h+1)**。`drawRect(6,12,40,30)` は列 6..46・行 12..42。
- 閉じたループは同じ画素を2度踏まない。a=128 の枠でも角と辺は同じ明るさ（194）で、
  **角の二重ブレンドは無い**。
- 幅0 の枠も描かれる（同じ線を往復するので、半透明なら二重ブレンドの明るさになる）。
  B5 の幅0の枠は 1x1 の枠と重なる位置にあり、重なり回数どおり 1回 194・2回 206・
  3回 208 の明るさになっている。

## 6. 合成 — 4チャンネルブレンドと rgb×a 表示

B2・B3 は素直な src-over では説明できない（平均誤差 26、最大 121）。次のモデルで
Mac・Windows とも平均誤差 1.0（8bit 換算）で一致した。

```
dst.rgb ← α·src.rgb + (1−α)·dst.rgb
dst.a   ← α·α       + (1−α)·dst.a        ← アルファにも同じブレンド式
表示値   = dst.rgb × dst.a
```

`glBlendFunc(SRC_ALPHA, ONE_MINUS_SRC_ALPHA)` を4チャンネルに掛け、モニターが
フレームバッファを黒の上に合成して見せている形。

| 画面 | 実機 | このモデル | src-over |
|---|---|---|---|
| 黒地に白 a=128 を1回 | 96 | 96 | 128 |
| 同じく 2 / 3 / 5 回重ね | 119 / 126 / 126 | 120 / 126 / 128 | 192 / 224 / 248 |
| 灰(128)地に白 a=51 | 129 | 129 | 153 |
| 灰(128)地に白 a=204 | 193 | 193 | 230 |
| 白地に黒 a=17 | 221 | 223 | 238 |

値は B1 の不透明グレーランプを較正曲線にして、画面輝度から setColor 相当値へ
戻したもの。

- a=128 を何度重ねても **128 で頭打ち**になる（src-over なら 255 に近づく）。
- a=0 は何も変えない。a=8 は変化が見える（下限は 1/255 刻み）。
- **drawClear はアルファを無視して置換**し、表示は rgb×a（B4：`setColor(0,255,0,128)`
  で全面が緑 128 相当）。前回「premultiply されている」と書いたのは、この表示側の
  掛け算を見ていた。
- B1 の伝達特性は Mac と Windows で数レベル違う（トーンマッピングの差）が、形は同じ。
  RGB 3色も同じ曲線。画面輝度はシーンのライティングを通った値なので、
  `setColor` の絶対値へ戻すには使えない。相対比較にだけ使っている。

## 7. 文字（B7）

- 4x5 グリフ、送り 5px、行送り 6px。小文字は大文字と同じ形。
- 小数座標は切り捨て（`drawText(0.5,…)` は x=0、`drawText(0,42.5,…)` は y=42）。
- 左端の見切れはそのまま描かれる（x=−3 の C は最後の1列だけ見える）。
- `drawTextBox` は語単位で折り返す。
- `media/pixelFont.js` と形が違うグリフ（見えている範囲で）：
  `C G I J Q T 1 2 7 ; ? { } | @ $ % & * /`。例えば C は実機 `.##./#..#/#.../#..#/.##.`、
  PhySim `.###/#.../#.../#.../.###`。A・0・[・H は x<2 のマスクにかかる位置なので
  比べていない。

## PhySim への反映状況

| 項目 | 状態 |
|---|---|
| 線・円・円塗り・三角形・三角形塗り | `media/raster.js` を実機規則に変更済み |
| drawRect / drawRectF | `raster.js` に `strokeRectangle` / `fillRectangle` を追加、`mcScreen.js` の RECT を切り替え済み |
| 合成（6節） | **未反映**。`media/blend.js` は src-over のまま |
| 重なった辺の二重ブレンド（5節の幅0の枠） | **未反映**。`mcScreen.js` の `plotter()` が1形状内で重複を除く |
| グリフ・drawTextBox の折り返し（7節） | **未反映**。`pixelFont.js` / `mcScreen.js` の担当 |

## 未確定のまま残っているもの

データでは決まらなかったので、実装は自然な側を選んで明記してある。

- 傾きちょうど 45° の線を x 主軸と y 主軸のどちらで扱うか（両方とも全ページ一致）。
  実装は x 主軸。
- GPU の 1/256 サブピクセル丸めの有無（有無どちらでも全ページ一致。1/16 より粗い
  丸めは不一致で棄却）。実装は丸め無し。
- drawRectF の小数 y、負の幅・高さ。
- 円の辺数の上限が 16 か、16..21 のどれか。
- 円の頂点の回り順。

## 撮影データの注意

- **C5 と C6 はスクリプトどおりの内容ではなかった。** ページ見出しは正しく "C5" "C6"
  と出ているが、C5 に r=15/15.25/15.5/15.75 の輪郭（本来 C6 の内容）、C6 に C9 と
  画素単位で同じ塗り円が描かれていた。ゲーム内に貼ったスクリプトがファイルと違って
  いたと思われる。そのため **中心の小数部の試験（本来の C5）は撮れていない**。
  中心 +0.5 の A2 は一致しているので規則自体は裏付けがあるが、+0.25 / +0.75 は未確認。
  fixture からは C5・C6 を除外している（内容から推測した描画命令でも一致はする）。
- `image/*/R1.jpg` は4枚モニターの別撮影で、カードの対象外。
- `image/*/old/` は旧 B カード（下端目盛なし）。今回の B1–B7 で置き換え済み。

## 次の追試

1. **C5 の撮り直し**（中心 +0.25 / +0.5 / +0.75 の r=22）。
2. drawRectF の小数 y と負の大きさ、drawTriangleF の標本点の再確認（塗り三角形を
   小数 y で数個）。
3. 45° の線と、ひし形の角をかすめる線（主軸の扱いとサブピクセル丸めを分ける）。
4. drawTextBox の揃え（B7 右の箱は中央揃えの計算が PhySim と合っていない可能性がある。
   未解析）。
